# Claude Codeへ渡す実装指示文

以下をそのままClaude Codeへ貼り付け、同時に `rail-route-builder_design.md` を参照させてください。

---

あなたは経験豊富なTypeScriptフルスタックエンジニアです。

添付の `rail-route-builder_design.md` を仕様の正本として、日本国内の鉄道旅行向けWebアプリ「鉄道路線プランナー」のMVPを実装してください。

## 今回の目的

利用者が複数の駅を自分の好きな順番で並べ、各駅で必要な時間を設定し、区間ごとの列車候補を選びながら一日の旅程を組めるWebアプリを作ります。

最初の成果物はAPIキーなしで完全に操作できるデモ版にしてください。実交通APIの有無で開発を止めないでください。

## 技術構成

- React
- TypeScript（strictを有効）
- Vite
- Cloudflare Workers
- WorkerのルーティングはHono
- 入力検証はZod
- 駅のドラッグ並べ替えはdnd-kit
- 状態管理はZustandまたはuseReducerのどちらか一つに統一
- テストはVitest
- UIは日本語
- スマートフォンとPCのレスポンシブ対応
- CSSはプレーンCSSまたはCSS Modulesでよい。UIライブラリを大量に追加しない

Cloudflare Workersの静的アセット配信とAPIを同一プロジェクトで扱える、現在の公式構成に沿ってください。古いPages専用構成を無理に使わないでください。

## 実装順序

### 1. 動くデモ版を完成させる

まず `MockProvider` を実装してください。

- 駅候補として、名古屋、大阪難波、吉野、京都、東京などを持たせる
- 任意の駅間に対して、出発可能時刻以降の候補を3件返す
- すべての結果に「デモデータ」と分かる表示を付ける
- APIキーがなくても、駅追加、並べ替え、検索、便選択、後続再計算、保存まで動かす

### 2. プロバイダーを交換可能にする

`TransitProvider` インターフェースを作り、ReactコンポーネントがGoogle等の固有レスポンスへ直接依存しないようにしてください。

環境変数 `TRANSIT_PROVIDER=mock|google` で切り替えられる構造にします。未設定時は必ず `mock` としてください。

### 3. 可能ならGoogleProviderを追加する

時間が許せば、Cloudflare Worker側に次を実装してください。

- 駅候補: Places API (New)
- 区間経路: Routes API `directions/v2:computeRoutes`
- APIキー: Workerの `GOOGLE_MAPS_API_KEY` Secretからのみ読む
- Transitでは中間経由地を指定せず、隣り合う駅ごとに1リクエストを送る
- `travelMode: "TRANSIT"`
- `computeAlternativeRoutes: true`
- `departureTime` をRFC3339で指定
- 表示言語は日本語、地域は日本
- 必要なFieldMaskだけを指定し、`routes.*` は本番コードで使わない
- GoogleのJSONを設計書の `RouteCandidate` / `TransitSegment` へ変換する
- 運賃が返らない場合は `fareYen: null` とする

GoogleProviderが未完成またはキー未設定でもMockProviderへ安全に戻り、画面が壊れないようにしてください。

## 必須UI

1. プラン名
2. 旅行日
3. 出発時刻
4. 駅検索と追加
5. 駅カードのドラッグ並べ替え
6. 上下ボタンでの並べ替え
7. 各途中駅の「この駅で必要な時間」入力
8. 駅の削除
9. 「時刻を検索」ボタン
10. 合計所要時間、移動時間、駅滞在時間、乗換回数、運賃
11. 駅と区間を縦につないだタイムライン
12. 各区間の複数候補と選択ボタン
13. 接続不能・経路なし・通信失敗を区別した警告
14. プラン保存、読込、新規作成
15. デモモード表示
16. データ取得時刻と公式情報確認の注意

## 重要な挙動

- 駅が3件なら区間は2件生成する。
- 最初の区間は入力した出発日時以降を検索する。
- 次区間の検索開始時刻は「直前区間の到着時刻 + その駅で必要な時間」とする。
- 手動で選んだ候補は `isLocked=true` とし、上流変更時に勝手に置換しない。
- 固定した便に間に合わなくなった時は `MISSED_CONNECTION` を表示する。
- 自動選択の後続区間だけを再検索する。
- 区間候補を変えた場合、その区間より前は再検索しない。
- 古い非同期結果が新しい状態を上書きしないよう、AbortControllerまたは検索世代IDを使う。
- 日付をまたぐ時刻を正しく扱う。
- 表示タイムゾーンは `Asia/Tokyo`。
- 運賃の一部が取得できない場合、合計を確定額として表示しない。

## 保存

localStorageへ保存するのは次の利用者入力を中心にしてください。

- プラン名
- 旅行日・出発時刻
- 駅の順番
- 各駅の必要時間
- メモ

外部APIから取得した時刻・運賃レスポンスは恒久保存せず、プランを開いた後に再検索させてください。Zustand persistを使う場合は `partialize` で検索結果を除外してください。

## API

最低限、次を実装してください。

- `GET /api/health`
- `POST /api/stations/search`
- `POST /api/routes/search`

すべてWorker側でもZod検証を行い、駅数・文字列長・日時を制限してください。エラーは共通形式 `{ code, message }` で返してください。

## セキュリティ

- APIキーを `VITE_` プレフィックスの環境変数へ入れない
- APIキーをブラウザへ返さない
- `.dev.vars` と `.env` を `.gitignore` へ追加
- `.env.example` または `.dev.vars.example` には変数名だけを書く
- 外部サイトの時刻表スクレイピングは実装しない
- ログへAPIキーや外部APIの完全なURLを出さない

## テスト

少なくとも次の単体テストを書いてください。

1. 駅数から正しい区間数を作る
2. 到着時刻と必要時間から次区間の検索時刻を計算する
3. 固定便に間に合わない場合に競合を返す
4. 未固定区間だけを再計算対象にする
5. 深夜0時をまたぐ
6. 一部運賃不明時に合計を確定扱いしない
7. 保存対象から候補結果が除外される

## ファイル構成の目安

```text
src/
  components/
  features/itinerary/
  state/
  lib/
  types/
worker/
  index.ts
  providers/
    transit-provider.ts
    mock-provider.ts
    google-provider.ts
  schemas/
tests/
```

実際のCloudflare/Vite構成に合わせて変更して構いませんが、責務を混ぜないでください。

## 完了条件

- `npm install` 後にローカル起動できる
- APIキーなしでMockProviderのデモを操作できる
- 2〜10駅を追加・削除・並べ替えできる
- 全区間の時刻が連鎖して表示される
- 便の変更と後続再計算ができる
- 保存・読込ができる
- 390px幅でも操作できる
- TypeScript、テスト、ビルドが成功する
- APIキーがクライアントバンドルへ含まれない

## 作業方法

1. 最初に設計書を読み、実装対象を短く整理する。
2. ファイルツリーを提示する。
3. Phase 1を最後まで実装する。
4. 型チェック、テスト、ビルドを実行し、失敗を修正する。
5. READMEにローカル起動、デモモード、Google API設定、Cloudflareデプロイ方法を書く。
6. 最後に、完成した機能、未実装項目、必要な環境変数を簡潔に報告する。

疑問点があっても、設計書内で合理的に決められるものは判断して進めてください。実交通APIの契約・課金・利用条件など、利用者の判断が必要な点だけを未実装として明記してください。疑似コードだけで終わらず、動くファイル一式を作成してください。

---
